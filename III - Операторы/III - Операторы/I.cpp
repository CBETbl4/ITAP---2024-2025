/*#include <iostream>
#include <cmath> 
using namespace std;
int main()
{
	float x, y;
	cin >> x >> y;
	if ((-(x) < sqrt(pow(8, 2) - pow(y, 2))) && (x < 0) && (-(x) > sqrt(pow(3, 2) - pow(y, 2)))) cout << "Yes";
	else if ((-(x) > sqrt(pow(8, 2) - pow(y, 2))) || (x > 0) || (-(x) < sqrt(pow(3, 2) - pow(y, 2)))) cout << "No";
	else cout << "On the border";
	return(0);
}*/